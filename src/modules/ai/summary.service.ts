export const generateDailySummaryAI = async (prompt: string) => {
  // TEMP MOCK — replace with OpenAI / local LLM later
  return {
    summary:
      "Today you made strong progress by resolving backend stability issues and implementing features to restore work context. This significantly improves continuity and sets up the foundation for intelligent task recommendations.",
  };
};
